    /*import java.awt.*;
import java.io.*;
import java.util.ArrayList;
import javax.swing.JButton;
import javax.swing.JFrame;
import javax.swing.JLabel;
import javax.swing.JOptionPane;
import javax.swing.JPanel;
import javax.swing.JTextField;
import javax.swing.SwingUtilities;
*/
    
    /*
    private static final ArrayList<Game> allGames = new ArrayList<>();
    
    public static void main (String[] args){
        SwingUtilities.invokeLater(() -> {
            JFrame frame = new JFrame("7 Wonders Player Stats");
            frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
            frame.setSize(400, 200);
            frame.setLayout(new FlowLayout());

            JButton newGameButton = new JButton("Create New Game");
            JButton loadGameButton = new JButton("Load Previous Games");

            newGameButton.addActionListener(e -> createNewGame(frame));
            loadGameButton.addActionListener(e -> {
                loadGamesFromFile();
                StringBuilder sb = new StringBuilder();
                for (Game g : allGames) sb.append(g.toString()).append("\n");
                    JOptionPane.showMessageDialog(frame, sb.toString().isEmpty() ? "No games loaded." : sb.toString());
            });

            frame.add(newGameButton);
            frame.add(loadGameButton);

            frame.setVisible(true);
        });
    }

    private static JPanel createPlayerInputPanel(JTextField[] fields, String playerNameLabel) {
        JPanel panel = new JPanel(new GridLayout(9, 2));  // 8 fields + name label
        panel.add(new JLabel("Player Name:"));
        fields[0] = new JTextField();
        panel.add(fields[0]);

        String[] labels = { "War", "Coins", "Wonder", "Civilian", "Commercial", "Guilds", "Scientific" };
        for (int i = 0; i < labels.length; i++) {
            panel.add(new JLabel(labels[i] + " points:"));
            fields[i + 1] = new JTextField();
            panel.add(fields[i + 1]);
        }

        return panel;
        
    }

    private static void createNewGame() {
        System.out.println("Creating new game...");
        int gameNumber = 1;

        String input = JOptionPane.showInputDialog(frame, "How many players?");
        try {
            int numPlayers = Integer.parseInt(input);
            Game game = new Game(gameNumber, numPlayers);

        for (int i = 1; i <= numPlayers; i++) {
            JTextField[] fields = new JTextField[8]; // name + 7 score categories
            JPanel panel = createPlayerInputPanel(fields, "Player " + i);

            int result = JOptionPane.showConfirmDialog(frame, panel, "Enter stats for Player " + i, JOptionPane.OK_CANCEL_OPTION);

            if (result == JOptionPane.OK_OPTION) {
                try {
                    String name = fields[0].getText();
                    int war = Integer.parseInt(fields[1].getText());
                    int coins = Integer.parseInt(fields[2].getText());
                    int wonder = Integer.parseInt(fields[3].getText());
                    int civilian = Integer.parseInt(fields[4].getText());
                    int commercial = Integer.parseInt(fields[5].getText());
                    int guilds = Integer.parseInt(fields[6].getText());
                    int scientific = Integer.parseInt(fields[7].getText());

                    Player player = new Player(name, war, coins, wonder, civilian, commercial, guilds, scientific);
                    game.addPlayer(player);
                } catch (NumberFormatException ex) {
                    JOptionPane.showMessageDialog(frame, "Invalid input for player " + i);
                    i--; // retry current player
                }
            } else {
                JOptionPane.showMessageDialog(frame, "Game creation cancelled.");
             return;
            }
        }

        JOptionPane.showMessageDialog(frame, game.toString());
        allGames.add(game);
        } catch (NumberFormatException e) {
            JOptionPane.showMessageDialog(frame, "Invalid number of players.");
        }
    }

    private static void saveGamesToFile() {
        try (ObjectOutputStream out = new ObjectOutputStream(new FileOutputStream("7wonders_games.dat"))) {
            out.writeObject(allGames);
            JOptionPane.showMessageDialog(null, "Games saved successfully!");
        } catch (IOException e) {
            JOptionPane.showMessageDialog(null, "Error saving games: " + e.getMessage());
        }
    }
    
    private static void loadGamesFromFile() {
        try (ObjectInputStream in = new ObjectInputStream(new FileInputStream("7wonders_games.dat"))) {
            allGames.clear();
            allGames.addAll((ArrayList<Game>) in.readObject());
            JOptionPane.showMessageDialog(null, "Games loaded successfully!");
        } catch (IOException | ClassNotFoundException e) {
            JOptionPane.showMessageDialog(null, "Error loading games: " + e.getMessage());
        }
    }*/